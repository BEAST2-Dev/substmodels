<assembly xmlns="http://maven.apache.org/ASSEMBLY/2.2.0"
          xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
          xsi:schemaLocation="http://maven.apache.org/ASSEMBLY/2.2.0 http://maven.apache.org/xsd/assembly-2.2.0.xsd">
    <id>beast-package</id>
    <formats>
        <format>zip</format>
    </formats>
    <includeBaseDirectory>false</includeBaseDirectory>

    <!-- version.xml at ZIP root -->
    <files>
        <file>
            <source>${project.basedir}/version.xml</source>
            <outputDirectory>/</outputDirectory>
        </file>
    </files>

    <!-- Project JAR + its own runtime deps (exclude BEAST core and its transitive deps) -->
    <dependencySets>
        <dependencySet>
            <outputDirectory>/lib</outputDirectory>
            <useProjectArtifact>true</useProjectArtifact>
            <scope>runtime</scope>
            <excludes>
                <exclude>io.github.compevol:beast-base</exclude>
                <exclude>io.github.compevol:beast-fx</exclude>
                <exclude>io.github.compevol:beast-pkgmgmt</exclude>
                <!-- Or be more specific: -->
                <exclude>org.openjfx:*:*:*</exclude>
            </excludes>
            <useTransitiveFiltering>true</useTransitiveFiltering>
        </dependencySet>
    </dependencySets>

    <fileSets>
        <!-- examples/ (if present) -->
        <fileSet>
            <directory>${project.basedir}/src/test/resources/${beast.pkg.name}/examples</directory>
            <outputDirectory>/examples</outputDirectory>
        </fileSet>
        <!-- fxtemplates/ (if present; use JPMS module-namespaced resource path) -->
        <fileSet>
            <directory>${project.basedir}/src/main/resources/${beast.pkg.name}/fxtemplates</directory>
            <outputDirectory>/fxtemplates</outputDirectory>
        </fileSet>
    </fileSets>
</assembly>
